// loops through possible moves, if no moves are possible it is checkmate
export function isCheckmate(possibleMoves) {
  const moves = []

  board.forEach(tile => {
    if (tile.piece.color === turn) {
    }
  })

  return !moves.length
}
